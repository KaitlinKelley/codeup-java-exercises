package shapes;

//This is an interface, so all methods are abstract
//All properties are public static and final
//No static methods!

public interface Measurable {

    double getPerimeter();
    double getArea();

}
